fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
anthropic>=0.49.0
httpx>=0.27.0

# Market Regime bridge (backend/modules/regime/) — similarity_engine.py,
# validation.py, and factor_schema.py in ../Market Regime/ depend on these.
# pyarrow must match (or be newer than) whatever wrote
# Market Regime/data/factors_weekly.parquet, or reading it raises
# "OSError: Repetition level histogram size mismatch" — an older pyarrow
# can't read a parquet file written by a newer one.
pandas>=2.3.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=24.0.0

# Research_LLM bridge (backend/modules/research/arxiv_bridge.py) —
# arxiv_client.py in ../Research_LLM/ depends on these for Paper2Alpha's
# search/download/analyze endpoints. chromadb + sentence-transformers back
# the semantic abstract/section search specifically — without them,
# search_local_papers_by_topic / search_sections_by_topic fail with
# "Phase 4 requires additional packages. Run: pip install chromadb
# sentence-transformers" (see Research_LLM/requirements.txt, which is the
# source of truth for this list — kept in sync manually).
mcp>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
markdownify>=0.13.0
chromadb>=1.0.0
sentence-transformers>=5.0.0
pypdf>=6.0.0
